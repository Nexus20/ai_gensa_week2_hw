#!/usr/bin/env bash
# UserPromptSubmit: injects a one-line project conventions reminder
# Exit 0, message on stdout — shown to the agent before the turn starts

echo "Orbital Ops conventions — pure logic → src/domain/ (tested), shared hooks → src/hooks/, config → src/config.ts, types → src/api/. Fix the code, not the checks."
exit 0
